# Contract fixtures

Pinned response bodies from NENE2 `docs/openapi/openapi.yaml` examples (system routes).

Update when the upstream OpenAPI example values change; record revision in `openapi-pin.txt`.
